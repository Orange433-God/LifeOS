{
  "name": "lifeos",
  "version": "0.1.0",
  "private": true,
  "description": "LifeOS — 愿此身，行至山海。人生操作系统（阶段 1：项目骨架）",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n ai,api,web -c yellow,cyan,magenta \"npm run dev:ai\" \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:ai": "node scripts/start-ai-service.mjs",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "build": "npm run build -w backend && npm run build -w frontend",
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w frontend",
    "db:generate": "npm run db:generate -w backend",
    "db:migrate": "npm run db:migrate -w backend",
    "db:deploy": "npm run db:deploy -w backend",
    "db:studio": "npm run db:studio -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
